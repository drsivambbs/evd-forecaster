# Data-entry gate credentials (committed at user request; repo is public).
# The Firebase service-account key is deliberately NOT here - it stays in
# Streamlit Cloud Secrets and the gitignored .secrets/ key file.
data_entry_user = "icmrnie"
data_entry_password = "icmrnie123"
